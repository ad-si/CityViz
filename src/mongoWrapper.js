var mongodb = require('mongodb'),
	mongoWrapper = {}


mongoWrapper.createTable = function () {
	// TODO
	// Creates the buildings table
}

mongoWrapper.insert = function (data) {

	// TODO
	// Inserts data into buildings table
}

mongoWrapper.getAll = function(){

	// TODO
	// Returns promise which will eventually resolve
	// to an array of buildings
}

module.exports = mongoWrapper
